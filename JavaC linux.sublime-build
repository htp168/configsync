{
    "shell_cmd": "rm -rf ${folder}/target/classes/* && cp -r ${folder}/src/main/resources/* ${folder}/target/classes/ && find ${folder} -name *.java -type f > ${folder}/sources.list && javac -cp .:${folder}/target/lib/* -d ${folder}/target/classes @${folder}/sources.list",
    "working_dir": "${project_path:${folder}}",
    "file_regex": "^(...*?):([0-9]*):?([0-9]*)",
    "selector": "source.java",
    "encoding": "utf-8"
}